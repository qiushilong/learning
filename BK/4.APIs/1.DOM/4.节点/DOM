DOM(Document Object Model)，是W3C组织推荐的处理可扩展标记语言(HTML,XML)的标准编程接口
W3C已经定义了一系列的DOM接口，通过这些DOM接口可以改变网页的内容，结构，样式

文档：一个页面就是一个文档，DOM种用document表示
元素：页面中的所有标签都是元素，DOM中使用element
节点：网页中的所有内容都是节点（标签，属性，文本，注释等），DOM中用node表示
DOM把以上元素都可以看成对象

DOM树
文档->根元素<html>->元素<...>->结点
